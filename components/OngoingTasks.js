import { StyleSheet, Text, View, Image, TouchableOpacity, TextInput, FlatList } from 'react-native';

const OngoingTasks = () => { 
   return(
         <View>
            <View>
             <Text style={{marginTop: 40, marginLeft: 20, fontSize:25, fontWeight: 'bold'}}>OngoingTasks</Text>
            </View>
            
            </View>
);
};

   export default OngoingTasks;